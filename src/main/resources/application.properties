spring.application.name=NovaMart

# Database Configuration (using environment variables with fallbacks)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/novamart}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1234}
spring.jpa.hibernate.ddl-auto=update

# JWT Configuration (using environment variables with fallbacks)
app.jwt.secret=${JWT_SECRET:scQ.9Zm2t=fqNbXRqzmf0g,j+Md>L.gqEtv(vVg_a_]}
app.jwt.expiration-hours=${JWT_EXPIRATION_HOURS:6}

# Disable stack traces and show only clean messages
server.error.include-stacktrace=never
server.error.include-message=always
server.error.include-binding-errors=never
server.error.include-exception=false

# Optional: Custom error page settings
server.error.path=/error

# Spring Security (for basic auth - remove if using JWT)
spring.security.user.name=user
spring.security.user.password=1234